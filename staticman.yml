comments:
  allowedFields: ["name", "email", "message"]
  branch: "master"
  commitMessage: "New Comment"
  filename: "entry{@timestamp}"
  format: "yaml"
  generatedFields:
    date:
      type: date
      options:
        format: "iso8601"
  moderation: true
  name: "aidan-duggan.github.io/whilegaming"
  path: "_data/comments/{options.slug}"
  requiredFields: ["name", "email", "message"]
  moderation: true
  transforms:
    email: md5
